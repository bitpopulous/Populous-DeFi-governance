const {
    deployMockPPT, 
    deployMockPXT, 
    deployMockVotingToken
} = require('../helpers/deploy');

describe('MockPPT deployment', accounts => {
    xit('should deploy with valid contract address');
    xit('should deploy with valid metadata - name, symbol, decimals');
});

describe('MockPXT deployment', accounts => {
    xit('should deploy with valid contract address');
    xit('should deploy with valid metadata - name, symbol, decimals');
});

describe('MockVotingToken deployment', accounts => {
    xit('should deploy with valid contract address');
    xit('should deploy with valid metadata - name, symbol, decimals');
    xit('should deploy with correct owner/admin');
});